# Tarea Unidad 2!

En la tarea de la unidad 2: Calculadora de Estaciones,
Se opto por utilizar la función prompt de javascript para desplegar una alerta con solicitud de texto y así obtener  el dato ingresado por el usuario
Luego de esto se utilizo un if-else para validar el ingreso de un valor adecuado.
Dentro de este if se utilizo un if-else-if para mostrar una alerta con cada estacion dependiendo de el mes ingresado 
por el usuario. Finalmente se muestra una alerta con mi nombre y numero de carnet.